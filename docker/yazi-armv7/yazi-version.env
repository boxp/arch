# IS01 (armv7) 向けにビルドする yazi のバージョン。
#
# yazi は armv7 のバイナリを配布していない (aarch64 / i686 / riscv64 /
# sparc64 / x86_64 のみ) ので、こちらでクロスビルドする。
#
# Renovate が sxyazi/yazi の GitHub Release を追って自動で更新する。
# 更新 PR がマージされると build-yazi-armv7.yml が発火してビルドされる。
YAZI_VERSION=26.8.15
