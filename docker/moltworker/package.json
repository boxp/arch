{
  "name": "moltworker-deploy",
  "version": "0.2.0",
  "private": true,
  "description": "Moltworker - OpenClaw on Cloudflare Workers + Containers (upstream clone method)",
  "scripts": {
    "prepare": "bash prepare.sh",
    "deploy": "cd .upstream && npx wrangler deploy",
    "deploy:dry-run": "cd .upstream && npx wrangler deploy --dry-run",
    "dev": "cd .upstream && npx wrangler dev"
  }
}
