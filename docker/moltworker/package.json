{
  "name": "moltworker-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Moltworker - OpenClaw on Cloudflare Workers + Containers",
  "scripts": {
    "build": "echo 'Using upstream moltworker build'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/sandbox": "*",
    "moltworker": "github:cloudflare/moltworker"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
