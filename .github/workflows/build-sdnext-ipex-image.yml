name: Build SD.Next IPEX Image

on:
  push:
    branches: [main]
    paths: ['docker/sdnext-ipex/**', '.github/workflows/build-sdnext-ipex-image.yml']
  pull_request:
    paths: ['docker/sdnext-ipex/**', '.github/workflows/build-sdnext-ipex-image.yml']
  workflow_dispatch:
    inputs:
      sdnext_repo:
        description: 'SD.Next source repository'
        required: true
        default: 'https://github.com/vladmandic/sdnext.git'
      sdnext_ref:
        description: 'SD.Next source ref'
        required: true
        default: 'master'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Resolve source
        id: source
        env:
          INPUT_SDNEXT_REPO: ${{ inputs.sdnext_repo }}
          INPUT_SDNEXT_REF: ${{ inputs.sdnext_ref }}
        run: |
          repo="${INPUT_SDNEXT_REPO:-https://github.com/vladmandic/sdnext.git}"
          ref="${INPUT_SDNEXT_REF:-master}"
          echo "repo=$repo" >> "$GITHUB_OUTPUT"
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ghcr.io/boxp/arch/sdnext-ipex
          tags: |
            type=raw,value={{date 'YYYYMMDDHHmm'}},enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: docker/sdnext-ipex
          push: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
          build-args: |
            SDNEXT_REPO=${{ steps.source.outputs.repo }}
            SDNEXT_REF=${{ steps.source.outputs.ref }}

